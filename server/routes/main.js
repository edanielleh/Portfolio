const express = require("express");
const router = express.Router();
// require all routes files here const Example = require("./example.js");

// router.use("/api", Example);

module.exports = router;